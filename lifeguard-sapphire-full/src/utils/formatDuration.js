function formatDuration(ms){ if(ms==null) return 'Never'; let sec=Math.floor(ms/1000); const parts=[]; const days=Math.floor(sec/86400); sec%=86400; const hours=Math.floor(sec/3600); sec%=3600; const minutes=Math.floor(sec/60); sec%=60; if(days) parts.push(`${days} day${days!==1?'s':''}`); if(hours) parts.push(`${hours} hour${hours!==1?'s':''}`); if(minutes) parts.push(`${minutes} minute${minutes!==1?'s':''}`); if(sec) parts.push(`${sec} second${sec!==1?'s':''}`); return parts.length?parts.join(' '):'0 seconds'; } module.exports={formatDuration};